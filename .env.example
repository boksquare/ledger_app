# Copy to .env next to docker-compose.yml and fill in. NEVER commit the real .env.

# Required only for statement import (Docker). Generate with: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=

# Optional: model/effort for statement parsing (defaults to your Claude Code defaults)
LEDGER_AI_MODEL=sonnet
LEDGER_AI_EFFORT=low
